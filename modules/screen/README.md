# screen

Reusable screen and image steps belong here, including window capture, region
capture, cropping, window fitting, preprocessing, and image saving.

Page-specific coordinates and evidence-workbook behavior do not belong here;
keep those values in profiles and Excel operations in `modules/excel/`.
