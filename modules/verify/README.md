# verify

Pure parsing, matching, normalization, and assertion capabilities belong here.
Prefer deterministic functions with no UI, COM, or hidden process state.

Capture mechanics, persistence, and workflow-specific policy do not belong
here; use the corresponding capability module or profile.
