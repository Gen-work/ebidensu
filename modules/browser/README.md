# browser

Reusable foreground-browser steps belong here, including navigation, keyboard
input, page-text reads, waits, and browser-window discovery.

Site-specific page identities, selectors, and business decisions belong in a
profile or workflow, not in browser step implementations.
