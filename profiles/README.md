# profiles

Project and system profiles belong here: page definitions, grammars, rules,
key strategy, and reusable layout data. Profiles follow
`docs/ebi-dance/spec/PROFILE-SCHEMA.md`.

Operator secrets, generated runtime state, and executable step code do not
belong here.
