# TODO — 2026-05-29 (office)

## 0. First thing: sync repo to local

```powershell
cd <your local repo>
git pull origin claude/practical-maxwell-XvKaA
.\Fix-Encoding.ps1          # re-add BOM after git pull strips it
```

---

## 1. ⚠️ CHECK JenkinsSnap.ps1 (URGENT)

Your local JenkinsSnap.ps1 was rewritten at some point to a shorter version that calls
functions that **do not exist** in Common.ps1:
  - Get-EdgeHwnd
  - Activate-Window
  - Resize-Window
  - Capture-Window

**If GiftJenkins / GfixJenkins / NoGfix crash → restore from repo:**
```powershell
git checkout origin/claude/practical-maxwell-XvKaA -- JenkinsSnap.ps1
.\Fix-Encoding.ps1
```
(The repo version is the full 429-line working version.)

---

## 2. Fix JIGPMB1S log (GFIX受信log still TODO)

The log wasn't being moved before (JOB_NAME wasn't 13-digit, old code skipped).
Now GfixLogDownload uses timestamp detection — should work.

```powershell
# Step 1: re-download the log
.\VerifyTool.ps1 -Phase GfixLog -TargetIds JIGPMB1S -Force

# Step 2: after log is in work\log\JIGPMB1S.log, replace evidence
.\VerifyTool.ps1 -Phase ReplaceGfix -TargetIds JIGPMB1S -Force

# Step 3: highlight the Command: line in yellow
.\VerifyTool.ps1 -Phase MarkGfixLog -TargetIds JIGPMB1S -Force
```

Expected Command: line in the log:
  `Command: '/appl/IGP2/shell/IGPLB073run.sh ... /appl/IGP2/IGP2Ver1/gfix/recv/JIGPRB1S R'`

---

## 3. DfSnap — set df.exe path

Option A (permanent): edit `VerifyConfig.psd1`
```
Df = @{
    ExePath = 'C:\actual\path\to\df.exe'   # <-- set this
    ...
}
```

Option B: just type the path when prompted each time DfSnap runs.

---

## 4. Normal phase progress check

Run Status to see where things stand:
```powershell
.\VerifyTool.ps1 -Phase Status
```

Phases still likely pending for some rows:
- GfixLogDownload (JIGPMB1S and any others not yet done)
- DfSnap (needs df.exe configured)
- MarkGfixLog (after ReplaceGfix is done)
- Clone → Replace → Mark → Review pipeline

---

## 5. GoAnywhere max rows reminder (manual for now)

Before starting GfixLogDownload, set GoAnywhere page to **100 rows** (default=20, not enough).
Auto-setting is a future TODO.

---

## Notes from today's fixes (2026-05-28)

| What | Why fixed |
|---|---|
| GfixLogDownload: log not moved | Old code required 13-digit JOB_NAME; fixed with timestamp detection |
| GfixLogDownload: CSV GFIX_log not set to 1 | Same root cause |
| GfixLogDownload: terminal stays in Edge | Added SetForegroundWindow to console HWND at end |
| DfSnap: hard crash if no DfExePath | Changed to interactive Read-Host prompt |
| GFIX受信log TODO in evidence | Downstream of move fix — re-run ReplaceGfix -Force after log downloaded |
